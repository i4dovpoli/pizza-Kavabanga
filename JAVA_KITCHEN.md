# Java панель замовлень

## 1. Запусти сайт через Flask

```powershell
cd D:\Kavabanga
.\start-flask.ps1
```

Сайт відкривається тут:

```text
http://127.0.0.1:5000
```

## 2. Запусти Java-програму кухні

В іншому терміналі:

```powershell
cd D:\Kavabanga
.\run-kitchen.ps1
```

Або подвійним кліком по `run-kitchen.bat`.

## Що вміє

- показує всі замовлення з сайту;
- показує код, ім'я клієнта, статус, суму й склад;
- кнопка `Прийняти замовлення` змінює статус на `Прийнято кухнею`;
- сайт має онлайн-чек за адресою `/receipt/<код>`.
